-- upgrade --
CREATE TABLE IF NOT EXISTS "pack" (
    "id" CHAR(36) NOT NULL  PRIMARY KEY,
    "name" VARCHAR(120) NOT NULL,
    "description" TEXT,
    "created" TIMESTAMP NOT NULL  DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE IF NOT EXISTS "puzzle" (
    "id" CHAR(36) NOT NULL  PRIMARY KEY,
    "name" VARCHAR(64),
    "created" TIMESTAMP NOT NULL  DEFAULT CURRENT_TIMESTAMP,
    "pack_id" CHAR(36) NOT NULL REFERENCES "pack" ("id") ON DELETE CASCADE
);
CREATE TABLE IF NOT EXISTS "word" (
    "text" VARCHAR(64) NOT NULL  PRIMARY KEY
);
CREATE TABLE IF NOT EXISTS "wordpuzzle" (
    "id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    "role" VARCHAR(5),
    "puzzle_id" CHAR(36) NOT NULL REFERENCES "puzzle" ("id") ON DELETE CASCADE,
    "word_id" VARCHAR(64) NOT NULL REFERENCES "word" ("text") ON DELETE CASCADE
);
CREATE TABLE IF NOT EXISTS "aerich" (
    "id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    "version" VARCHAR(255) NOT NULL,
    "app" VARCHAR(20) NOT NULL,
    "content" JSON NOT NULL
);
